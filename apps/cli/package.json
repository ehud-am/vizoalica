{
  "name": "@vizoalica/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The `vizoalica` command as published to npm. `scripts/build-package.mjs` assembles the publishable folder in dist/ from package.json.template.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
