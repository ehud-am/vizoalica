{
  "name": "@vizoalica/deploy-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "vizoalica-deploy": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --import tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
